<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Diesel-Rocket Blog Example</title>
  </head>
  <body>
    <h1>{{ postcount }}</h1>
    {% for post in posts%}
      <h3>{{ post.title }}</h3>
      <p>{{ post.body }}</p>
    {% endfor %}
  </body>
</html>
